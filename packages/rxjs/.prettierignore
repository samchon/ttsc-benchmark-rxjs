# Build artifacts left by tshy / typescript --watch that prettier --check
# would otherwise flag. Not committed (and not in upstream .gitignore
# because they live next to the .ts sources under src/), but they trip
# `prettier --check 'src/**/*.ts'` during the benchmark.
**/*.d.ts
**/*.d.ts.map
**/*.js
**/*.js.map
**/*.tsbuildinfo
